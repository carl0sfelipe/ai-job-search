# Verify 2026-09-21

Assumption: this is a personalized public fork of MadsLorentzen/ai-job-search (MIT). Production path is the agent workflow (`/setup` `/scrape` `/rank` `/apply`); Python tools + portal CLIs are the executable core; Danish portal CLIs are shipped demos and are disabled. No deploy target. `reports/` stays gitignored (`security_guards.py` requires the rule); these care reports are force-added. Branch name is `cursor/fix-2026-09-21-profile-setup-c650` because the cloud agent requires `cursor/<name>-c650` (user asked `fix/<data>-<tema>`).

## Identity

- Purpose: human-in-the-loop job search (evaluate fit, draft CV/letter, human sends). README.md.
- Stack: Markdown agent specs, Python 3.10+ stdlib tools, Bun/TypeScript portal CLIs, LaTeX (lualatex CV, xelatex cover).
- Build: `lualatex cv/main_example.tex`; `xelatex cover_letters/cover_example.tex`; `bun run typecheck` per CLI.
- Test: `python3 tools/lint_skills.py`; `python3 tools/security_guards.py`; `python3 -m unittest discover -s tests -t .`; `bun test` per CLI.
- Run: `claude` then `/setup` `/scrape` `/apply <url>`.
- Deploy: none. CI in `.github/workflows/ci.yml` is the only automated check.
- Production: `.claude/`, `.agents/skills/*/cli`, `tools/`, `cv/main_example.tex`, `cover_letters/`. Experiment/demo: Danish portals (`enabled: false`), `documents/` (empty gitkeeps).

## Baseline (before any edit)

- lint_skills PASS. `python3 tools/lint_skills.py` → `lint_skills: OK (9 skills, 12 commands, settings.json)` exit 0.
- framework_version PASS. `python3 tools/check_framework_version.py` → `Framework Version Check: OK` exit 0.
- security_guards PASS. `python3 tools/security_guards.py` → `security_guards: OK (permissions allowlist, hooks allowlist, gitignore rules, package manifests)` exit 0.
- python tests PASS. `python3 -m unittest discover -s tests -t . -v` → `Ran 231 tests in 32.413s` `OK` exit 0.
- CLI typecheck+test PASS (all 6). `bun run typecheck` + `bun test` in each `.agents/skills/*/cli` → freehire 37, jobbank 22, jobdanmark 26, jobindex 32, jobnet 23, linkedin 35; 0 fail.
- CV compile PASS. `lualatex -interaction=nonstopmode -halt-on-error cv/main_example.tex` → `Output written on main_example.pdf (2 pages, 39192 bytes)` exit 0.
- Cover compile PASS. `xelatex -interaction=nonstopmode -halt-on-error cover_letters/cover_example.tex` → `Output written on cover_example.pdf (1 page)` exit 0.
- PDF text layer PASS. `python3 tools/verify_pdf.py cv/main_example.pdf --min-chars 100` and cover equivalent → `Verified` exit 0.

## Findings

- `/apply` fact-source `cv/main_example.tex` still has setup placeholders. `.claude/commands/apply.md` Grounding Audit treats it as one of three sources of truth. `rg -n 'YOUR_NAME|Job Title|your.email@example.com' cv/main_example.tex` → `\name{[First]}{[Last]}`, `\email{[your.email@example.com]}`, `[Job Title]`.
- `/apply` cover-letter voice file is unfilled. apply.md Step 3 reads `02-behavioral-profile.md`. `rg -n 'YOUR_NAME|PROFILE_TYPE|DRIVE_1|BEHAVIOR_1' .claude/skills/job-application-assistant/02-behavioral-profile.md` → `[YOUR_NAME]'s behavioral assessment... **[PROFILE_TYPE]**`, `[DRIVE_1]`, `[BEHAVIOR_1]`.
- Fit framework life-situation lines still tokens. `rg -n 'YOUR_FINANCIAL|YOUR_SCHEDULE|YOUR_GROWTH' .claude/skills/job-application-assistant/04-job-evaluation.md` → `[YOUR_FINANCIAL_SITUATION_CONTEXT]`, `[YOUR_SCHEDULE_CONSTRAINTS]`, `[YOUR_GROWTH_PRIORITIES]`.
- STAR stubs empty in `07-interview-prep.md` (Situation/Task/Action/Result blank). Filling them would invent interview stories. Owner decision; not in plan.
- No committed secrets. `rg -i '(api[_-]?key|secret|token|password)\s*[:=]\s*['\''"][A-Za-z0-9_\-]{12,}'` (ex node_modules) → no matches.
- No generated files tracked by mistake. `git ls-files -i -c --exclude-standard` → empty.
- No urgent TODO/FIXME. `rg -n 'TODO|FIXME|XXX|HACK'` (ex node_modules) → only `SETUP.md` mktemp `XXXXXX` and phone `[+XX XXXXXXXXXX]`.
- Fork CI has never run. `gh api repos/carl0sfelipe/ai-job-search/actions/runs --jq .total_count` → `0`. Workflows CI and Upstream watch are `active`.
- No critical dependency vulns. `bun audit` in freehire/linkedin → `No vulnerabilities found`. Danish CLIs (disabled): `2 vulnerabilities (1 moderate, 1 low)` via `@bunli/core` → `diff@8.0.2` GHSA-73rr-hh4g-fpgx, `file-type@16.5.4` GHSA-5v7r-6r5c-r473. Not critical; upgrading `@bunli` is a large dep change, out of plan.
- `reports/` is a required ignore rule. `rg -n '"reports/"' tools/security_guards.py` → REQUIRED_IGNORE_RULES. Do not negate it.

## Plan (max 4; right-sized)

1. Fill `cv/main_example.tex` from `docs/CV.md` + `01-candidate-profile.md` (no invented metrics). Prove: `lualatex` exit 0 and `pdfinfo` Pages 2; `pdftotext -layout` shows email and Deloitte. Revert: `git checkout -- cv/main_example.tex`.
2. Fill `.claude/skills/job-application-assistant/02-behavioral-profile.md` from CLAUDE.md Behavioral Profile (self-assessment only; no invented PI/DISC type). Prove: file has no `[YOUR_NAME]`/`[PROFILE_TYPE]`/`[DRIVE_1]`. Revert: `git checkout --` that file.
3. Fill life-situation lines in `04-job-evaluation.md` from CLAUDE.md constraints/goals. Prove: no `[YOUR_FINANCIAL_SITUATION_CONTEXT]`. Revert: `git checkout --` that file.
4. Add `tests/test_profile_setup_tokens.py` so those leftover tokens fail CI on this fork. Prove: `python3 -m unittest tests.test_profile_setup_tokens -v` exit 0. Revert: delete the test file.

Out of plan: redesign, framework swap, `@bunli` upgrade, enabling Danish portals, filling STAR stubs, `/add-portal` for Gupy, changing production/DNS/deploy.
