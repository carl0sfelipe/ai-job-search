# Report 2026-09-21

Assumption: cloud agent branch prefix `cursor/` + suffix `-c650` overrides the requested `fix/<data>-<tema>`. `reports/` stays gitignored; this file is force-added.

## Before

- lint/guards/python/CLI/LaTeX: all pass (231 python tests; 6 CLIs green).
- `/apply` fact-source `cv/main_example.tex`: stock placeholders.
- `/apply` voice file `02-behavioral-profile.md`: stock `[YOUR_NAME]` / `[PROFILE_TYPE]` / `[DRIVE_1]`.
- `04-job-evaluation.md` life situation: `[YOUR_FINANCIAL_SITUATION_CONTEXT]` etc.
- No critical vulns. Danish CLIs (disabled): 1 moderate + 1 low via `@bunli`.
- Fork Actions: 0 runs. No secrets. No stray generated files.

## After

- Same commands plus 3 new tests. `python3 -m unittest discover -s tests -t .` → 234 OK.
- `lualatex cv/main_example.tex` → 2 pages; `pdftotext` has `carlosfelipe2050@gmail.com`, Deloitte, full name.
- `02` / `04` leftover tokens gone.
- `python3 tools/lint_skills.py` / `security_guards.py` still OK.

## Done (commit → change)

- fill leftover `/setup` tokens in the files `/apply` actually reads (02, 04, master CV) so drafts cannot leak placeholders or fail the grounding audit against empty template facts.
- add `tests/test_profile_setup_tokens.py` so those tokens fail CI on this fork.
- record verify + this report; CHANGELOG Unreleased.

## Ready to deploy

- Nothing deploys. Merge to `master` is the only publish step. Human merge; do not auto-submit applications.
- First Actions run will be this PR (`pull_request` on `ci.yml`).

## Decisões do dono

- Fill STAR S/T/A/R in `07-interview-prep.md` with real stories (left blank; inventing is forbidden).
- Display nameplate wraps (`Carlos Felipe Siqueira` / `Batista Soares`) at 34pt. Keep, shorten the visible name, or shrink `\namefont`.
- `/add-portal` for Gupy / Programathor (called out in `search-queries.md`).
- Upgrade `@bunli` on disabled Danish CLIs (moderate/low, not critical).
- Enable GitHub Dependency graph so `dependency-review` is real, not skipped.

## Risks

- Master CV is now personal data on a public repo. Intentional (public fork + `docs/CV.md` already public).
- Nameplate wrap may look tight; page count is 2.
- Future upstream pull of stock `main_example.tex` / `02` will fail the new test until re-filled.
- `@bunli` vulns unchanged (portals `enabled: false`).
